#/usr/bin/env bash

TMP=$(mktemp)

for file in $*; do
    echo -n "# of lines (= states) before: "
    cat $file | wc -l

    sort -u $file > $TMP

    mv -f $TMP $file

    echo -n "# of lines (= states) after:  "
    cat $file | wc -l
done
